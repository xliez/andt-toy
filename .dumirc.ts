import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  themeConfig: {
    name: '@xliez/antd-toy',
    logo: false,
  },
  base: '/andt-toy/',
  publicPath: '/andt-toy/',
});
